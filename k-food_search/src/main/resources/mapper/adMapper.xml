<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.myapp.dao.AdDAO">
	<select id="adSelect" resultType="AdVO">
		select bannerimg from ad_table where status like '승인';
	</select>
	<select id="infoSelect" resultType="AdVO">
		select 
	</select>
</mapper>